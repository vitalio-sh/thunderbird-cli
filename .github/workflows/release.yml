name: release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: |
          npm test
          npm run test:mcp

      - name: Build XPI from source
        run: npm run build:xpi

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Find release XPI
        id: xpi
        run: |
          # The Thunderbird extension version (in manifest.json) is independent of
          # the npm/release version. We attach the newest release XPI in dist/releases/.
          SIGNED_XPI=$(find dist/releases -name "*-tb.xpi" -type f | sort -V | tail -1)
          if [ -z "$SIGNED_XPI" ]; then
            echo "::error::No release XPI found in dist/releases/"
            echo "Expected: dist/releases/*-tb.xpi"
            echo "Add the XPI returned by addons.thunderbird.net to dist/releases/ before tagging."
            exit 1
          fi
          echo "signed=$SIGNED_XPI" >> "$GITHUB_OUTPUT"
          echo "Found release XPI: $SIGNED_XPI"
          UNSIGNED_XPI=$(find dist -maxdepth 1 -name "thunderbird-cli-*.xpi" -type f | sort -V | tail -1)
          echo "unsigned=$UNSIGNED_XPI" >> "$GITHUB_OUTPUT"
          echo "Found source-built XPI: $UNSIGNED_XPI"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ steps.version.outputs.version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## thunderbird-cli v${{ steps.version.outputs.version }}

            ### Install the Thunderbird extension

            Download the attached **`thunderbird_ai_bridge-*-tb.xpi`** (the build distributed through addons.thunderbird.net, byte-identical to `extension/` at this tag; no embedded signature; the extension has its own version number) and install via:
            1. Thunderbird → **Add-ons and Themes**
            2. ⚙ gear icon → **Install Add-on From File…**
            3. Select the downloaded `.xpi`

            ### Install the CLI and MCP server

            ```bash
            git clone https://github.com/vitalio-sh/thunderbird-cli
            cd thunderbird-cli
            npm install
            ```

            See [docs/SETUP.md](https://github.com/vitalio-sh/thunderbird-cli/blob/v${{ steps.version.outputs.version }}/docs/SETUP.md) for the full setup guide.

            ### Changelog

            See [CHANGELOG.md](https://github.com/vitalio-sh/thunderbird-cli/blob/v${{ steps.version.outputs.version }}/CHANGELOG.md) for details.
          files: |
            ${{ steps.xpi.outputs.signed }}
            ${{ steps.xpi.outputs.unsigned }}
