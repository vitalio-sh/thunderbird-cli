# Thunderbird Bridge connection
# In Docker: use host.docker.internal
# On host: use 127.0.0.1 (default)
TB_BRIDGE_HOST=127.0.0.1
TB_BRIDGE_PORT=7700

# Optional: auth token for bridge
# TB_AUTH_TOKEN=
